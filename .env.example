# Guitar Chord Creator - Environment Variables
# Copy this file to .env.local and fill in your actual values
# NEVER commit .env or .env.local to git!

# Storage Provider
# Options: "local" (browser localStorage) or "s3" (S3-compatible storage)
VITE_STORAGE_PROVIDER=local

# S3-Compatible Storage Configuration
# Only required if VITE_STORAGE_PROVIDER=s3
# Works with AWS S3, MinIO, Backblaze B2, Azure Blob Storage (via S3 API), etc.

# S3 Endpoint (without trailing slash)
# AWS S3: https://s3.amazonaws.com or https://s3.<region>.amazonaws.com
# MinIO: http://localhost:9000 or https://minio.yourdomain.com
# Backblaze B2: https://s3.us-west-002.backblazeb2.com
VITE_S3_ENDPOINT=

# S3 Bucket Name
VITE_S3_BUCKET=

# S3 Region
# AWS regions: us-east-1, eu-west-1, etc.
# MinIO: usually "us-east-1" (default)
VITE_S3_REGION=

# S3 Access Credentials
# ⚠️ SECURITY WARNING: These are sensitive credentials!
# For production, use IAM roles or temporary credentials instead
VITE_S3_ACCESS_KEY_ID=
VITE_S3_SECRET_ACCESS_KEY=

# Optional: Prefix for stored charts (folder path)
# Default: "chord-charts"
VITE_S3_PREFIX=chord-charts
