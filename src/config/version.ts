/**
 * Application version configuration
 * 
 * Version format: MAJOR.MINOR.PATCH
 * - MAJOR: Breaking changes
 * - MINOR: New features (backwards compatible)
 * - PATCH: Bug fixes and small changes (auto-increment per commit)
 * 
 * NOTE: This version is automatically synced with package.json at build time.
 * To update the version, change it in package.json (root level).
 */

export const APP_VERSION = '3.2.3';
