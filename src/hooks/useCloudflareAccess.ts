import { useEffect } from "react";

export function useCloudflareAccess() {
  useEffect(() => {
    // Optionally, check for Cloudflare Access cookie or redirect if not present
    // This is a placeholder; actual protection is handled at the edge
  }, []);
}
